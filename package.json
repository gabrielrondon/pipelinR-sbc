{
  "name": "pipelinr-sbc",
  "version": "0.1.0",
  "scripts": {
    "start": "dfx start --background && dfx deploy SmartBlockCanister",
    "build": "dfx build",
    "deploy": "dfx deploy",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@dfinity/agent": "^0.15.6",
    "@dfinity/candid": "^0.15.6",
    "@dfinity/principal": "^0.15.6",
    "react": "^17.0.2",
    "react-dom": "^17.0.2"
  },
  "devDependencies": {
    "@tailwindcss/forms": "^0.5.9"
  }
}
